const configured=()=>window.SUPABASE_URL&&!window.SUPABASE_URL.includes('COLE_AQUI')&&window.SUPABASE_ANON_KEY&&!window.SUPABASE_ANON_KEY.includes('COLE_AQUI');
const sb=configured()?supabase.createClient(window.SUPABASE_URL,window.SUPABASE_ANON_KEY):null;
const demoMsg='Supabase ainda não configurado em assets/js/config.js. O painel abrirá em modo demonstração.';
async function init(){ if(!sb){console.warn(demoMsg); showDash(); renderAdmin([]); return;} const {data:{session}}=await sb.auth.getSession(); if(session) showDash(); }
function showDash(){loginView.classList.add('hidden');dashView.classList.remove('hidden');loadDash()}
loginForm.addEventListener('submit',async e=>{e.preventDefault(); if(!sb){alert(demoMsg);showDash();return} const {error}=await sb.auth.signInWithPassword({email:email.value,password:password.value}); if(error) return alert('Erro no login: '+error.message); showDash();});
logout.addEventListener('click',async()=>{if(sb)await sb.auth.signOut();location.reload()});
async function loadDash(){ if(!sb){renderAdmin([]);return} const {data}=await sb.from('properties').select('*').order('created_at',{ascending:false}); renderAdmin(data||[]); const {data:leads}=await sb.from('leads').select('*').order('created_at',{ascending:false}); leadList.innerHTML=(leads||[]).map(l=>`<p><b>${l.nome}</b> - ${l.telefone} - ${l.tipo||''}</p>`).join('')||'<p>Nenhum lead.</p>' }
function renderAdmin(items){ stTotal.textContent=items.length; stDisp.textContent=items.filter(i=>i.status==='disponivel').length; stVend.textContent=items.filter(i=>i.status==='vendido').length; stAlug.textContent=items.filter(i=>i.status==='alugado').length; adminList.innerHTML=items.map(i=>`<div><b>${i.titulo}</b><span>${i.status} • ${i.finalidade}</span></div>`).join('')||'<p>Nenhum imóvel cadastrado ainda.</p>' }
propertyForm.addEventListener('submit',async e=>{e.preventDefault(); const raw=Object.fromEntries(new FormData(e.target)); const payload={...raw,valor:Number(raw.valor||0),area:Number(raw.area||0),quartos:Number(raw.quartos||0),banheiros:Number(raw.banheiros||0),vagas:Number(raw.vagas||0)}; if(!sb){alert(demoMsg);return} const {error}=await sb.from('properties').insert(payload); if(error)return alert(error.message); alert('Imóvel salvo!'); e.target.reset(); loadDash();});
settingsForm.addEventListener('submit',async e=>{e.preventDefault(); const data=Object.fromEntries(new FormData(e.target)); if(!sb){alert(demoMsg);return} const {error}=await sb.from('site_config').upsert({id:1,...data,updated_at:new Date().toISOString()}); if(error)return alert(error.message); alert('Configurações salvas!')});
init();
